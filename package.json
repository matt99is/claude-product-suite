{
  "name": "claude-product-suite",
  "version": "0.7.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check skills/figma-writing/helpers/figma-helpers.js && npm run release:check",
    "release:check": "node scripts/check-release.js"
  }
}
